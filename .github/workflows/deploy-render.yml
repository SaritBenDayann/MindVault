name: Deploy Backend to Render

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deployment
        run: |
          echo "Triggering secure deployment on Render..."
          curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
